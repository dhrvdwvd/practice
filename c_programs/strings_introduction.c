#include<stdio.h>

/*****************************************************
*    A string is a character array which is term-    *
*    inated by a NULL character (\0).                *
*****************************************************/

int main() {
  
  char str[] = {'A', 's', 't', 'r', 'i', 'n', 'g', '\0'};
  // or //
  char str[]="Astring";
  return 0;

}