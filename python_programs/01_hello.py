import os
# The above is a built-in module.
print("Hello world!")
# Some external modules: flask, tensorflow, ... these are installed
# using pip, the package manager.
#
# Running "python" command on terminal opens Python REPL
# (Read-Evaluate-Print-Loop), i.e., it reads, then evaluates,
# and then prints, and returns back to reading.